<!-- <!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <title>Signup</title>
  <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/modern-normalize/modern-normalize.min.css">
  <style>
    body{font-family:Inter,Segoe UI,Tahoma,Geneva,Verdana,sans-serif;padding:40px;max-width:400px;margin:auto}
    input,button{width:100%;padding:10px;margin:6px 0;font-size:1rem}
    button{cursor:pointer}
  </style>
</head>
<body>
  <h1>Signup</h1>
  <form action="/user" method="post">
    <input name="name"     placeholder="Full name" required />
    <input name="email"    type="email"    placeholder="Email"    required />
    <input name="password" type="password" placeholder="Password" required />
    <button>Create account</button>
  </form>
  <p>Already registered? <a href="/login">Login</a></p>
</body>
</html> -->



<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <title>Signup • ShortURL</title>
  <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@300;400;500&display=swap" rel="stylesheet">
  <style>
    :root{--pri:#635bff;--pri-dark:#5549ff;--radius:14px}
    *{box-sizing:border-box;margin:0;padding:0}
    body{
      font-family:'Poppins',sans-serif;display:flex;justify-content:center;align-items:center;
      min-height:100vh;background:linear-gradient(135deg,#dfe4ff 0%,#faf7ff 100%);
      color:#1f2457;padding:16px
    }
    .panel{
      width:100%;max-width:420px;background:#fff;padding:42px 36px;border-radius:var(--radius);
      box-shadow:0 18px 30px rgba(37,44,97,0.08);animation:zoom .5s
    }
    @keyframes zoom{from{transform:scale(.93);opacity:0}}
    h1{font-size:1.7rem;font-weight:600;margin-bottom:28px;text-align:center}
    label{font-size:.9rem;margin-bottom:6px;color:#60647e}
    input{
      width:100%;padding:14px 16px;font-size:1rem;margin-bottom:18px;
      border:1px solid #e1e5ff;border-radius:var(--radius);transition:border .25s
    }
    input:focus{outline:none;border-color:var(--pri)}
    .btn{
      width:100%;padding:14px 0;border:none;border-radius:var(--radius);
      background:var(--pri);color:#fff;font-weight:500;cursor:pointer;
      transition:background .2s,transform .15s
    }
    .btn:hover{background:var(--pri-dark)}
    .btn:active{transform:scale(.97)}
    .link{display:block;text-align:center;margin-top:18px;font-size:.9rem}
    .link a{color:var(--pri)}
  </style>
</head>
<body>
  <div class="panel">
    <h1>Create Account</h1>
    <form action="/user" method="post">
      <label>Full name</label>
      <input name="name" required>
      <label>Email</label>
      <input name="email" type="email" required>
      <label>Password</label>
      <input name="password" type="password" required>
      <button class="btn">Sign up</button>
    </form>
    <div class="link">Have an account? <a href="/login">Login</a></div>
  </div>
</body>
</html>
